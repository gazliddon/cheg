(ns cheg.server-spec
  (:require [speclj.core :refer :all]
            [cheg.server :refer :all]
            [cheg.obj :refer :all]
            ))

(describe "Server test"
          (it "should always pass"
              (should= 0 0 )))
